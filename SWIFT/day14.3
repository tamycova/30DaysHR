    init(a : [Int]){
        elements = a
    }

    func computeDifference() {
        var min = Int.max
        var max = Int.min
        for a in elements{
            if a < min { min=a }
            if a > max { max=a }
        }
        maximumDifference = max-min
    }
